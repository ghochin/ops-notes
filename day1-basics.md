# 第 1 天：云服务器入门

## 你的服务器信息

- **IP 地址**：8.163.121.232
- **系统**：Ubuntu 24.04.2 LTS
- **配置**：2 核 2G，40G 硬盘，200M 带宽
- **类型**：阿里云轻量应用服务器
- **登录方式**：`ssh root@8.163.121.232`

---

## 一、SSH 是什么

SSH（Secure Shell）是一种加密的网络协议，用来远程登录 Linux 服务器。你本地电脑通过 SSH 客户端连接到远在阿里云机房的服务器，所有传输内容都是加密的。

**连接过程**：
1. 你在本地终端输入 `ssh root@8.163.121.232`
2. 你的电脑去互联网上找到 8.163.121.232 这台服务器
3. 服务器说"你是谁，证明一下"
4. 你输入密码，验证通过后进入服务器的命令行
5. 你敲的命令都在服务器上执行，结果传回你屏幕上

**首次连接会问 yes/no**：因为你的电脑第一次连这台服务器，不认识它的"指纹"。你输入 yes 后指纹被保存在本地，下次就不用确认了。这个指纹是为了防止中间人攻击——如果有人冒充你的服务器，指纹会不一样。

---

## 二、root 用户是什么

`root` 是 Linux 系统的超级管理员账号，类似于 Windows 的 Administrator。

**root 的特点**：
- **最高权限**：可以读写/删除/修改任何文件
- **没有限制**：能干任何事，但也意味着能干任何坏事
- **没有二次确认**：执行 `rm -rf /` 会把整个系统删干净，不会问"确定吗？"
- **生产环境极少用 root**：日常操作应该用普通用户，需要时再临时提权（sudo）

---

## 三、Linux 目录结构

你当前在 `/root`，这是 root 用户的家目录。

Linux 的目录是一个倒置的树，根是 `/`：

| 目录 | 用途 | 常用吗 |
|------|------|--------|
| `/` | 文件系统的根，一切从这里开始 | 少去 |
| `/root` | root 用户的家目录 | 你是 root 就在这 |
| `/home` | 普通用户的家目录，比如 `/home/zhangsan` | 经常 |
| `/etc` | 配置文件目录，nginx、ssh 等软件配置都在这里 | **经常** |
| `/var/log` | 日志文件目录，所有服务的日志都在这 | **经常** |
| `/usr` | 用户程序目录，装的大部分软件在这 | 偶尔 |
| `/tmp` | 临时文件，重启就清空 | 偶尔 |
| `/opt` | 第三方软件目录 | 偶尔 |

**两个特殊的表示**：
- `.` = 当前目录
- `..` = 上一级目录

---

## 四、apt 包管理器

`apt` 是 Ubuntu/Debian 系统的软件包管理器，相当于苹果的 App Store 或安卓的应用商店，不过是命令行的。

| 命令 | 作用 |
|------|------|
| `apt update` | 更新软件源列表（从镜像站拉最新的软件目录） |
| `apt install 软件名` | 安装软件 |
| `apt install 软件名 -y` | 安装软件，中间不问"确定吗"，直接 yes |
| `apt remove 软件名` | 卸载软件 |
| `apt search 关键词` | 搜索软件 |

**你装 Nginx 时发生了什么**：
1. `apt update` 从阿里云镜像站拉了一份最新的软件列表（你看到刷刷刷的那些）
2. `apt install nginx -y` 自动下载 nginx 和它的依赖（nginx-common），解包，配置，设置开机自启，启动服务
3. 安装结束后 nginx 已经在运行了

---

## 五、systemctl 管理服务

`systemctl` 是 systemd 的控制工具，systemd 是 Linux 的"大管家"，管理系统里所有后台服务。

| 命令 | 作用 | 什么时候用 |
|------|------|-----------|
| `systemctl status 服务名` | 看服务当前状态 | **最常用**，出问题先看这个 |
| `systemctl start 服务名` | 启动服务 | 手动启动 |
| `systemctl stop 服务名` | 停止服务 | 停服维护 |
| `systemctl restart 服务名` | 重启服务 | 改了配置文件后让新配置生效 |
| `systemctl enable 服务名` | 设置开机自启 | 确保服务器重启后这个服务自动起来 |
| `systemctl disable 服务名` | 取消开机自启 | 不想要自启了 |

**status 输出里重点看两样**：
- `Active: active (running)` → 服务在正常运行
- `Active: inactive (dead)` → 服务没跑
- `Active: failed` → 服务挂了，去看日志

---

## 六、进程（Process）概念

一个进程 = 一个正在运行的程序。一个程序可以跑多个进程。

**nginx 的 master-worker 模型**：

```
nginx master (root)
    ├── worker 1 (www-data)
    └── worker 2 (www-data)
```

- **master 进程**：用 root 运行，负责管理，比如读配置、启停 worker
- **worker 进程**：用低权限用户 www-data 运行，负责实际处理用户的请求
- **为什么这样设计**：安全隔离。万一 nginx 被黑客攻击，黑客拿到的是 www-data 权限，碰不了系统核心文件

---

## 七、端口（Port）概念

端口 = 网络服务的门牌号。一个 IP 地址有很多端口，不同的端口对应不同的服务。

| 端口 | 通常对应的服务 |
|------|---------------|
| 22 | SSH（你连服务器用的） |
| 80 | HTTP（网页，你现在 nginx 监听的） |
| 443 | HTTPS（加密网页） |
| 3306 | MySQL 数据库 |
| 6379 | Redis 缓存 |
| 8080 | 另一个常见的 Web 端口 |

**你看到的 `0.0.0.0:80`**：
- `0.0.0.0` = 监听所有网络接口，任何 IP 都能访问
- `:80` = 监听 80 端口
- `[::]:80` = IPv6 版本的 80 端口

---

## 八、日志（Log）概念

日志是程序运行时记录的事件，运维的核心工作之一就是看日志。

**nginx 有两种日志**：
- **access.log**：访问日志，记录每一次 HTTP 请求。谁、什么时候、从哪个 IP、请求了什么、返回了什么状态码
- **error.log**：错误日志，记录程序出错的信息。网站打不开了先看这里

**nginx 日志各字段含义**（以你的为例）：

```
117.183.231.177 - - [10/Jun/2026:19:05:22 +0800] "GET / HTTP/1.1" 304 0 "-" "Mozilla/5.0..."
```

| 字段 | 值 | 含义 |
|------|-----|------|
| 第 1 个 | `117.183.231.177` | 访问者的 IP 地址 |
| 第 2 个 | `[10/Jun/2026:19:05:22 +0800]` | 访问时间（北京时间 UTC+8） |
| 第 3 个 | `"GET / HTTP/1.1"` | 请求方式 GET，请求路径 /（首页），协议 HTTP/1.1 |
| 第 4 个 | `304` | HTTP 状态码，304 = 页面没变化、用浏览器缓存 |
| 第 5 个 | `"Mozilla/5.0..."` | User-Agent，记录了访问者的浏览器和操作系统 |

---

## 九、HTTP 状态码速查

| 状态码 | 含义 | 什么时候看到 |
|--------|------|-------------|
| **200** | OK，成功 | 一切正常 |
| **301/302** | 重定向 | 页面跳转到别的地方了 |
| **304** | 未修改 | 页面没变，浏览器直接用缓存 |
| **403** | Forbidden | 没权限访问 |
| **404** | Not Found | 请求的资源不存在（比如你浏览器请求 /favicon.ico 但服务器上没有） |
| **500** | Internal Server Error | 服务器内部出错了，去看 error.log |
| **502** | Bad Gateway | 网关错误，反向代理后面服务挂了 |

---

## 十、Nginx 配置文件结构

nginx 主配置文件在 `/etc/nginx/nginx.conf`，83 行。

**关键配置块**：

```
user www-data;                    ← worker 进程以什么用户运行
worker_processes auto;            ← worker 数量自动（等于 CPU 核数）
pid /run/nginx.pid;               ← 主进程的 PID 文件路径
error_log /var/log/nginx/error.log; ← 错误日志路径

events {
    worker_connections 768;       ← 每个 worker 最多同时连 768 个
}

http {
    access_log /var/log/nginx/access.log;  ← 访问日志路径
    include /etc/nginx/mime.types;         ← 文件类型定义
    include /etc/nginx/conf.d/*.conf;      ← 引入额外配置文件
    include /etc/nginx/sites-enabled/*;    ← 引入站点配置（最重要！）
}
```

**站点配置在哪**：
- `/etc/nginx/sites-available/`：所有可用的站点配置文件（仓库）
- `/etc/nginx/sites-enabled/`：已启用的站点配置（软链接指向 sites-available）
- 要启用一个站点：在 sites-enabled 里建一个软链接指向 sites-available 里的配置
- 要禁用一个站点：删掉 sites-enabled 里的软链接

---

## 十一、你学会的命令大全

### 系统信息类
| 命令 | 功能 | 示例 |
|------|------|------|
| `pwd` | 显示当前目录 | `/root` |
| `ls -la` | 列出当前目录所有文件（含隐藏） | 看到 .bashrc .profile 等 |
| `whoami` | 显示当前用户名 | `root` |
| `cat /etc/os-release` | 查看系统版本 | Ubuntu 24.04.2 LTS |

### 文件操作类
| 命令 | 功能 |
|------|------|
| `cat 文件名` | 查看文件全部内容 |
| `wc -l 文件名` | 统计文件行数 |
| `ls 目录` | 列出目录下的文件 |
| `tail -f 文件名` | 实时追踪文件末尾新增内容（Ctrl+C 退出） |

### 软件管理类
| 命令 | 功能 |
|------|------|
| `apt update` | 更新软件源列表 |
| `apt install 软件名 -y` | 安装软件 |
| `systemctl status 服务名` | 查看服务状态 |

### 进程与网络类
| 命令 | 功能 |
|------|------|
| `ps aux` | 列出所有进程 |
| `ps aux \| grep 关键词` | 筛选包含关键词的进程 |
| `ss -tlnp` | 查看所有监听的 TCP 端口 |
| `ss -tlnp \| grep 关键词` | 筛选特定端口 |

### 重要概念

**管道 `|`**：把一个命令的输出传给另一个命令。比如 `ps aux | grep nginx` = 先列出所有进程，然后从中筛选包含 nginx 的行。

**Ctrl+C**：中断当前正在运行的命令。日志追踪、死循环、卡住了都按这个退出。

---

## 十二、常见的陷阱

1. **密码不显示是正常的**：SSH 登录输密码时屏幕上不会有任何反馈（没有 `****`），但你的输入是有效的。打完直接回车就行。

2. **root 权限没有撤回**：删东西前先确认路径，养成习惯。

3. **改配置后要重载**：改了 nginx 配置文件后要用 `systemctl reload nginx` 让新配置生效，不是改了自动生效。

4. **日志不会自动清理**：长期运行的服务器日志会越来越大，生产环境需要配置日志轮转（logrotate）。

5. **防火墙规则**：你在阿里云用的是轻量服务器，自带防火墙配置在控制台。如果你以后用 ECS，默认 80 端口是开的，其他端口需要手动在安全组里开。
